{
  "buildCommand": "cd templates/006-crucible-web && npm install --include=dev --legacy-peer-deps && npm run build",
  "outputDirectory": "templates/006-crucible-web/.next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/unified",
      "schedule": "0 8 * * *"
    }
  ]
}
